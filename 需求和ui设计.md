# PD-L1 TPS 辅助分析系统需求与 UI 设计

## 1. 项目背景

本系统参考 Visiopharm PD-L1 APP, NSCLC 的产品逻辑，面向 PD-L1 免疫组化切片的 TPS 辅助判读场景，目标是帮助病理医生在全切片或局部 patch 中快速理解肿瘤细胞 PD-L1 表达情况。

Visiopharm 的核心能力包括：自动识别组织与肿瘤区域、检测 PD-L1 阳性/阴性肿瘤细胞、计算 Tumor Proportion Score（TPS），并通过细胞标注、阳性分布热图等可视化结果辅助医生复核。本项目不完全照搬临床级闭环系统，而是在现有推理数据基础上突出“可解释、可复核、可对比”的可视分析能力。

## 2. 当前数据说明

当前数据位于 `code/data`，为单个 WSI 的推理结果导出，不包含人工 GT。

| 文件/目录 | 说明 | UI 使用方式 |
| --- | --- | --- |
| `thumbnail.png` | WSI 缩略图 | 用于全局导航、patch 空间定位 |
| `wsi_summary.json` | WSI 总体信息、模型 checkpoint、阈值、导出说明 | 用于病例信息卡、模型信息面板 |
| `patches_manifest.csv` | 20 个 patch 的清单，包含 patch 级 TPS 预测、细胞数、分类桶 | 用于 patch 列表、统计图、空间概览 |
| `patches/<patch_id>/image.jpg` | patch 原图 | 用于局部图像查看 |
| `patches/<patch_id>/patch.json` | 单个 patch 的预测结果 | 用于 patch 详情页 |
| `patches/<patch_id>/cells.csv` | 单个 patch 的细胞级推理结果 | 用于细胞散点、阳性/阴性标注、热力图 |

关键字段：

| 字段 | 含义 | 可视化建议 |
| --- | --- | --- |
| `patch_pred_bucket` | patch 级 TPS 分类，取值为 `TPS_50` / `TPS_10` / `TPS_1` / `Negative` | 用颜色表示 patch 风险等级 |
| `patch_pred_tps` | patch 级 TPS 标量预测 | 用数值卡片、柱状图、排序列表展示 |
| `num_cells` | patch 内检测到的细胞数量 | 用于判断结果稳定性和样本量 |
| `x`, `y` | 细胞中心坐标，基于 patch 图像坐标系 | 用于在原图上叠加细胞点 |
| `center_prob` | 细胞定位 heatmap 概率 | 用点透明度或定位置信度展示 |
| `cell_pos_prob` | 细胞阳性概率 | 用颜色深浅、详情面板展示 |
| `cell_pred` | 基于阈值得到的阴/阳性分类 | 阳性红色，阴性蓝色 |

当前数据特点：

- 单个 WSI：`DI2025-030614_2025-04-21_14_59_40`。
- 一共导出 20 个 patch。
- patch 类型包含高阳性（`TPS_50`）、中低阳性（`TPS_10`、`TPS_1`）和阴性（`Negative`）。
- 导出阈值为 `cell_threshold_used_for_export_labels = 0.5`，模型记录的最佳细胞阈值为 `0.73`。
- 数据仅包含推理结果，不包含医生标注或真实 TPS，因此 UI 中应避免使用“诊断结论”“真实阳性率”等表述。

## 3. 系统定位

系统定位为 PD-L1 TPS 可视分析与医生复核辅助平台。

核心目标：

1. 在 WSI 层面帮助用户快速看到不同区域的 TPS 分布、阳性热点和阴性区域。
2. 在 patch 层面展示模型预测的 TPS、分类桶、细胞数量和阳性/阴性细胞空间分布。
3. 在 cell 层面展示每个细胞的位置、阳性概率、定位置信度和分类结果。
4. 支持医生对可疑区域、临界 TPS 区域、空间异质性区域进行重点复核。
5. 通过热图、局部放大、筛选排序、阈值调节等交互方式解释模型结果。

本系统应强调“AI 预分析 + 医生复核”，而不是替代病理医生自动签发。

## 4. 用户角色

| 用户角色 | 主要目标 | 关注内容 |
| --- | --- | --- |
| 病理医生 | 复核 PD-L1 TPS 结果 | 阳性/阴性细胞是否合理，热点区域是否可信，临界区域是否需要人工判断 |
| 科研人员 | 分析模型表现和空间分布 | patch 分类、细胞概率分布、不同阈值下 TPS 变化 |
| 演示评审用户 | 快速理解系统价值 | 总览图、热点定位、细胞级解释、案例对比 |

## 5. 核心功能需求

### 5.1 WSI 总览

功能说明：

- 展示 WSI 缩略图。
- 将 20 个 patch 按原始坐标位置映射到缩略图上。
- 用颜色表示 patch 的 TPS 分类：
  - `TPS_50`：深红，表示高阳性区域。
  - `TPS_10`：橙色，表示中等阳性区域。
  - `TPS_1`：黄色，表示低阳性区域。
  - `Negative`：蓝色或灰蓝，表示阴性区域。
- 鼠标悬停时显示 patch 简要信息：patch ID、预测 TPS、细胞数、分类桶。
- 点击 patch 后进入 patch 详情分析。

设计目的：

- 让用户快速看到阳性区域是否集中、是否存在空间异质性。
- 支持从全局快速跳转到高 TPS 或可疑区域。

### 5.2 TPS 统计面板

功能说明：

- 展示病例级概览：
  - WSI ID。
  - patch 总数。
  - 总细胞数。
  - 平均 patch TPS。
  - 最高 TPS patch。
  - 不同 TPS 桶的 patch 数量。
- 展示 TPS 分布图：
  - 横轴为 patch。
  - 纵轴为 `patch_pred_tps`。
  - 可按 TPS 从高到低排序。
- 展示分类桶占比：
  - `TPS_50` / `TPS_10` / `TPS_1` / `Negative` 的数量与比例。

设计目的：

- 支持医生先看总体风险，再进入局部图像。
- 支持答辩或展示时快速说明数据分布。

### 5.3 Patch 详情视图

功能说明：

- 展示当前 patch 原图。
- 在原图上叠加细胞检测结果：
  - PD-L1 阳性细胞：红色圆点或红色描边。
  - PD-L1 阴性细胞：蓝色圆点或蓝色描边。
  - 点大小可固定，也可与 `center_prob` 关联。
  - 点透明度可与 `cell_pos_prob` 或 `center_prob` 关联。
- 支持图层开关：
  - 原图。
  - 阳性细胞。
  - 阴性细胞。
  - 细胞定位置信度。
  - 阳性概率热图。
- 展示 patch 级结果：
  - patch ID。
  - `patch_pred_bucket`。
  - `patch_pred_tps`。
  - `num_cells`。
  - 使用阈值 `cell_threshold_used`。

设计目的：

- 让用户看到 TPS 预测背后的细胞证据。
- 避免只给一个数值而缺少可解释性。

### 5.4 细胞级解释面板

功能说明：

- 点击单个细胞点后，在右侧面板展示：
  - `cell_id`。
  - 坐标 `x`, `y`。
  - `cell_pred`。
  - `cell_pos_prob`。
  - `center_prob`。
- 对接近阈值的细胞进行特殊提示：
  - 例如 `cell_pos_prob` 在 0.45 到 0.55 之间时标记为“临界细胞”。
- 支持按阳性概率筛选细胞：
  - 只看高置信阳性。
  - 只看临界细胞。
  - 只看阴性细胞。

设计目的：

- 解释“为什么这个细胞被判为阳性或阴性”。
- 帮助医生关注最可能影响 TPS 的细胞。

### 5.5 阳性热点与热图

功能说明：

- 根据细胞阳性概率生成局部热图。
- 热图可叠加在 patch 原图上。
- 支持在 WSI 总览中显示高 TPS patch 的空间聚集区域。
- 可将高阳性区域定义为：
  - `patch_pred_bucket = TPS_50`，或
  - `patch_pred_tps >= 0.5`。

设计目的：

- 对应 Visiopharm 中“positive cells distribution heat map”的功能。
- 帮助医生判断阳性细胞是否局灶聚集、是否存在空间异质性。

### 5.6 阈值调节与敏感性分析

功能说明：

- 提供细胞阳性阈值调节滑块。
- 默认使用导出阈值 `0.5`。
- 可标注模型建议阈值 `0.73`。
- 当阈值变化时，实时更新：
  - 阳性/阴性细胞分类。
  - 当前 patch 的细胞级阳性比例。
  - 阳性细胞数量。
  - 临界细胞数量。

设计目的：

- 当前数据存在导出阈值与模型最佳阈值两个值，UI 应把这个差异转化为可解释功能。
- 对临界 TPS 或弱阳性区域，阈值敏感性比单一结果更有展示价值。

### 5.7 Patch 列表与筛选

功能说明：

- 以表格或卡片形式展示 20 个 patch。
- 支持按以下字段排序：
  - `patch_pred_tps`。
  - `num_cells`。
  - `patch_pred_bucket`。
  - 阳性细胞数量。
  - 临界细胞数量。
- 支持按分类桶筛选：
  - 只看 `TPS_50`。
  - 只看 `TPS_10`。
  - 只看 `TPS_1`。
  - 只看 `Negative`。
- 点击列表项时同步定位到 WSI 缩略图和 patch 详情。

设计目的：

- 让用户快速找到最有分析价值的区域。
- 支持案例演示时从高阳性、低阳性、阴性 patch 之间切换对比。

### 5.8 医生复核与备注

功能说明：

- 支持医生对 patch 添加备注：
  - “模型结果可信”。
  - “疑似非肿瘤细胞干扰”。
  - “弱阳性/临界区域，建议复核”。
  - “热点区域，建议纳入展示”。
- 支持人工修改 patch 状态：
  - 未复核。
  - 已复核。
  - 需进一步确认。
- 可记录被医生重点关注的细胞或区域。

设计目的：

- 符合“AI 预分析 + 医生复核”的工作模式。
- 为后续病例分析或模型改进留下依据。

## 6. 非功能需求

| 类型 | 要求 |
| --- | --- |
| 可解释性 | 所有 TPS 和分类结果都应能回溯到 patch 与 cell 级数据 |
| 交互性能 | 20 个 patch 数据量较小，切换 patch 和筛选细胞应接近实时 |
| 可扩展性 | 后续可扩展到多个 WSI、多病例列表和 GT 对照 |
| 数据透明 | 明确标注当前数据为推理结果，不含 GT，不直接输出诊断结论 |
| 视觉一致性 | 阳性统一使用红色，阴性统一使用蓝色，低可信或临界结果使用黄色/紫色 |
| 临床谨慎性 | 界面文案使用“辅助分析”“模型预测”“建议复核”，避免“确诊”“最终判定”等表达 |

## 7. UI 总体布局（按参考图改版）

本版 UI 模仿你给的两张参考图，采用“左信息面板 + 右双层病理视图”的结构。右侧不再是单一画布，而是拆成两块：

- 右侧左半区：当前系统主显示的大图（WSI 背景图）。
- 右侧右半区：导航与联动区（底层大图 + 右上角视野框 + 右下角 patch 三图）。

```text
┌──────────────────────────────────────────────────────────────────────┐
│ 顶部栏：系统标题 / WSI ID / 阈值滑块 / 小图显示模式                  │
├────────────────────┬─────────────────────────────────────────────────┤
│ 左侧信息区          │ 右侧病理显示区                                  │
│ ┌ 上部：整张图数据 ┐│ ┌ 左：主大图(可点击) ┐ ┌ 右：导航联动区 ┐       │
│ │ WSI统计/分布/图例 ││ │ 点击后红框patch范围 │ │ 底层大图+右上视野框 │       │
│ └─────────────────┘│ └──────────────────┘ │ 右下patch三图面板 │       │
│ ┌ 下部：当前patch  ┐│                      └──────────────────┘       │
│ │ patch.json/cells ││                                                 │
│ │ 三图状态与明细    ││                                                 │
│ └─────────────────┘│                                                 │
└────────────────────┴─────────────────────────────────────────────────┘
```

### 7.1 左侧信息区（固定两段）

#### 左侧上部：整张大图数据（WSI 级）

显示内容：

- WSI 基本信息：`wsi_id`、`num_exported_patches`、模型阈值（`0.5` 与 `0.73`）。
- 全局统计：patch 总数、总细胞数、平均 `patch_pred_tps`、最高/最低 TPS。
- 全局分布：`TPS_50` / `TPS_10` / `TPS_1` / `Negative` 数量和占比。
- 图层控制（全局）：`cell_class`、`center_prob`、`heatmap_overlay` 三个开关。
- 小图显示模式（你要求）：
  - `全部显示`（3 图并排）。
  - `只显示 cell_class`。
  - `只显示 center_prob`。
  - `只显示 heatmap_overlay`。

#### 左侧下部：当前 patch 全部数据（Patch/Cell 级）

显示内容（全部来自 `code/data/case1`）：

- 当前 patch ID（如 `..._x6144_y30720`）。
- patch 级字段：`patch_pred_bucket`、`patch_pred_tps`、`num_cells`、`cell_threshold_used`。
- patch 在 `patches_manifest.csv` 对应行的核心字段（文件路径、预测桶、TPS）。
- `cells.csv` 汇总：
  - 阳性细胞数、阴性细胞数、阳性比例。
  - `cell_pos_prob` 均值/中位数。
  - 临界细胞数（接近阈值）。
- 当前 patch 的三张小图状态：
  - `center_prob.png`
  - `cell_class.png`
  - `heatmap_overlay.png`

## 8. 页面设计（单页联动版）

### 8.1 顶部控制栏

核心控件：

- 阈值滑块（默认 `0.5`，可切到 `0.73`）。
- patch 定位输入框（支持输入 patch ID 快速跳转）。
- 三图显示模式选择器（全部/单图）。
- 图层开关（`cell_class`、`center_prob`、`heatmap_overlay`）。

### 8.2 右侧左半区：主大图视图

功能要求：

- 底图显示整张 WSI（`thumbnail.png`）。
- 鼠标点击后，计算点击落在哪个 patch。
- 识别成功后，在主大图用红色线框标出该 patch 范围。
- 主大图支持平移、缩放、滚轮放大。
- 叠加可选图层：patch 分类色块、热点标记、当前选中框。

红框规范：

- 颜色：红色 `#FF3B30`。
- 线宽：2px（缩放后可保持视觉一致）。
- 只保留一个当前激活框。

### 8.3 右侧右半区：导航联动区

布局要求（模仿第二张图）：

1. 底层：整张 WSI 缩略导航图。
2. 右上角：当前主视图可视范围矩形框（viewport box）。
3. 底部右下：当前 patch 三张小图面板。

其中右下三图面板：

- 默认显示三图：
  - `cell_class`
  - `center_prob`
  - `heatmap_overlay`
- 当用户在左上选择“只显示某一图”时，右下只保留对应单图。
- 每张图固定 `512x512` 容器（按现有数据规格）。

### 8.4 左侧上部：整图数据卡

建议拆成四块卡片：

1. WSI 信息卡（ID、patch 数、阈值）。
2. 全局 TPS 统计卡（均值、最大值、最小值）。
3. 分类桶占比卡（`TPS_50`/`TPS_10`/`TPS_1`/`Negative`）。
4. 图层与小图显示控制卡（对应右侧联动）。

### 8.5 左侧下部：当前 patch 数据卡

建议拆成三块卡片：

1. patch 基本信息卡：`patch_id`、`patch_pred_bucket`、`patch_pred_tps`、`num_cells`。
2. 细胞统计卡：阳性/阴性数量、阳性比例、临界细胞数量。
3. 数据来源卡：
   - `patches/<patch_id>/patch.json`
   - `patches/<patch_id>/cells.csv`
   - `preview/by_patch/<patch_id>/*.png`

## 9. 视觉编码规范

| 对象 | 推荐颜色/样式 | 说明 |
| --- | --- | --- |
| PD-L1 阳性细胞 | 红色 `#E53935` | 对应 Visiopharm 中阳性细胞红色标注习惯 |
| PD-L1 阴性细胞 | 蓝色 `#1E88E5` | 对应阴性细胞蓝色标注 |
| 临界细胞 | 紫色或黄色描边 | 表示接近阈值，需要复核 |
| 高 TPS patch | 深红 | `TPS_50` |
| 中 TPS patch | 橙色 | `TPS_10` |
| 低 TPS patch | 黄色 | `TPS_1` |
| 阴性 patch | 灰蓝 | `Negative` |
| 热图高值 | 红/黄渐变 | 表示阳性概率集中 |
| 低可信区域 | 半透明斜纹或虚线边框 | 避免与阳性结果混淆 |

## 10. 交互流程（点击坐标定位 patch）

### 10.1 主流程

1. 页面加载后显示 WSI 底图和全局统计。
2. 用户在主大图点击任意位置 `(x_click, y_click)`。
3. 系统将显示坐标转换到 WSI 坐标系，匹配所属 patch。
4. 找到 patch 后：
   - 主大图绘制红色 patch 边框。
   - 左下数据区切换为该 patch 的完整信息。
   - 右下三图切换为该 patch 的 `cell_class/center_prob/heatmap_overlay`。
5. 用户切换“三图显示模式”时，右下小图即时更新（显示 3 张或单张）。
6. 用户拖动阈值时，左下细胞统计和状态即时刷新。

### 10.2 patch 匹配计算规则

`patch_id` 命名示例：

```text
DI2025-030614_2025-04-21_14_59_40_x6144_y30720
```

可从 `patch_id` 中解析 patch 左上角坐标：

- `patch_x = 6144`
- `patch_y = 30720`

假设 patch 尺寸为 `512x512`，则该 patch 覆盖区域为：

```text
[patch_x, patch_x + 512) × [patch_y, patch_y + 512)
```

点击坐标归属判定：

```text
若 patch_x <= x_click < patch_x + 512 且 patch_y <= y_click < patch_y + 512
则点击命中该 patch
```

当主图显示的是缩略图时，需要先做缩放换算：

```text
x_wsi = x_view / view_width  * wsi_width
y_wsi = y_view / view_height * wsi_height
```

再用 `(x_wsi, y_wsi)` 执行 patch 命中计算。

### 10.3 视野框联动规则（右上角）

- 右侧导航图右上角显示主大图当前可见范围框。
- 主大图平移/缩放时，右上视野框实时更新。
- 点击导航图时，可反向驱动主大图跳转到对应区域。

## 11. MVP 范围（按当前 UI 改版）

第一阶段优先实现：

1. 读取 `wsi_summary.json`、`patches_manifest.csv`、`preview/by_patch` 三图数据。
2. 单页完成左信息区 + 右双层病理视图布局。
3. 支持主大图点击坐标命中 patch，并绘制红色边框。
4. 命中后联动更新左下 patch 全部数据。
5. 命中后联动更新右下小图（3 图或单图模式）。
6. 支持右上视野框显示主图范围。
7. 支持阈值切换并刷新 patch 细胞统计。
8. 支持 patch ID 搜索定位。

第二阶段扩展：

1. 主图叠加 patch TPS 分级着色。
2. 临界细胞筛选与高亮。
3. patch 对比分析（双 patch 并排）。
4. 医生备注和复核状态。
5. 自动生成分析报告。

第三阶段扩展：

1. 多 WSI 病例列表。
2. GT 标注对照。
3. 肿瘤区域、坏死区域、间质区域等更多 path 级结构展示。
4. 模型版本对比。
5. PACS / IMS 工作流模拟。

## 12. 关键指标与计算方式

### 12.1 Patch 级 TPS

直接使用数据中的 `patch_pred_tps`，展示为百分比：

```text
Patch TPS = patch_pred_tps * 100%
```

### 12.2 细胞级阳性比例

基于当前阈值动态计算：

```text
细胞级阳性比例 = cell_pos_prob >= 当前阈值的细胞数 / 当前 patch 总细胞数
```

说明：

- 该值用于解释当前 patch 的细胞组成。
- 它不一定等同于模型输出的 `patch_pred_tps`，因为 patch 级 TPS 可能来自模型的整体预测。
- UI 中应区分“模型预测 TPS”和“按当前阈值统计的细胞阳性比例”。

### 12.3 临界细胞

推荐定义：

```text
abs(cell_pos_prob - 当前阈值) <= 0.05
```

临界细胞用于提示医生：这些细胞最容易因阈值变化而改变阳性/阴性分类。

## 13. 与 Visiopharm 设计逻辑的对应关系

| Visiopharm PD-L1 APP 功能 | 本系统对应设计 |
| --- | --- |
| 全切片自动分析 | WSI 总览与 patch 空间分布 |
| 样本组织与对照组织区分 | 后续可扩展组织区域/非目标区域图层 |
| 侵袭性肿瘤区域识别 | 后续可扩展肿瘤区域图层；当前以 patch 推理结果为主 |
| 肿瘤细胞阳性/阴性分类 | 细胞级红蓝标注、概率面板 |
| TPS 自动计算 | patch 级 TPS 预测与细胞级阳性比例 |
| 阳性分布热图 | patch 热图与 WSI 热点展示 |
| 病理医生复核签发 | 复核状态、备注、报告导出 |

## 14. 案例设计建议

### 案例一：高阳性热点区域

选择 `patch_pred_bucket = TPS_50` 且 `patch_pred_tps` 较高的 patch。

展示重点：

- 阳性细胞明显密集。
- TPS 数值高。
- 热图显示局部阳性热点。
- 说明系统如何帮助医生快速定位高表达区域。

### 案例二：阴性区域

选择 `patch_pred_bucket = Negative` 的 patch。

展示重点：

- 阴性细胞占绝大多数。
- `cell_pos_prob` 整体较低。
- 与高阳性 patch 对比，突出模型对区域差异的识别能力。

### 案例三：弱阳性/临界区域

选择 `patch_pred_bucket = TPS_1` 或 `TPS_10` 的 patch。

展示重点：

- 部分细胞接近阈值。
- 阈值从 0.5 调整到 0.73 后，阳性细胞数量可能明显变化。
- 说明医生复核机制的必要性。

## 15. 页面文案建议

系统标题：

```text
PD-L1 TPS 辅助分析系统
```

副标题：

```text
基于 WSI、Patch 与 Cell 三级结果的 PD-L1 表达可视分析
```

重要提示：

```text
当前结果为模型推理结果，仅用于辅助分析与可视化复核，不作为独立诊断依据。
```

按钮文案：

- 查看热点区域
- 查看临界细胞
- 切换热图
- 调整阈值
- 添加复核备注
- 导出分析报告

## 16. 总结

本系统的设计重点不是简单复现一个“阳性率计算器”，而是将 PD-L1 TPS 判读拆解为 WSI 总览、patch 分析、cell 解释和医生复核四个层次。相比只输出 TPS 数值的方式，本系统更强调空间分布、细胞证据、阈值敏感性和复核流程，能够更好地展示 AI 在 PD-L1 IHC 判读中的辅助价值。
